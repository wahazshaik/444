from django.urls import include, path 
from rest_framework.routers import DefaultRouter
#from .views import BankMasterViewSet,PartyMasterViewSet,PlantMasterViewSet,LCDocumentTypeMasterViewSet


# router = DefaultRouter()
# router.register('bank', BankMasterViewSet)
# router.register('party', PartyMasterViewSet)
# router.register('plant', PlantMasterViewSet)
# router.register('lc-document', LCDocumentTypeMasterViewSet)

urlpatterns = [
    #path('app-status/', app_status, name='app-status'),
    #path('', include(router.urls)),
]